<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WhatsappCloneApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primaria</item>
        <item name="actionOverflowButtonStyle">@style/ActionOverflowButton</item>

    </style>

    <!-- Criando um stilo para as bolinhas do menu da toolbar-->
    <style name="ActionOverflowButton" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name="tint">@color/white</item>
    </style>

    <style name="Theme.WhatsappCloneApp" parent="Base.Theme.WhatsappCloneApp" />

    <style name="Circulo">
        <item name="cornerSize">50%</item>
    </style>
</resources>